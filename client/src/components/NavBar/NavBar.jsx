import SearchBar from "./SearchBar";
import Buttons from './Buttons'

const NavBar = () => {
    return (
        <div className="navBar">
            <SearchBar />
            <Buttons />
        </div>
    );
};

export default NavBar;